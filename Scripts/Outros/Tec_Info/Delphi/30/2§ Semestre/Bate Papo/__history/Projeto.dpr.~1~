program Projeto;

uses
  Forms,
  Unidade1 in 'Unidade1.pas' {Login},
  Unidade3 in 'Unidade3.pas' {Cadastrar},
  Unidade2 in 'Unidade2.pas' {Formulario},
  Unit1 in 'Unit1.pas' {DM: TDataModule};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TLogin, Login);
  Application.CreateForm(TCadastrar, Cadastrar);
  Application.CreateForm(TFormulario, Formulario);
  Application.CreateForm(TDM, DM);
  Application.Run;
end.
